// This Prisma schema is not actively used as the project's primary database is Firestore.
// It was part of a previous setup or exploration with a relational database.
// If you intend to use Prisma with a SQL/NoSQL database it supports,
// you will need to uncomment and configure the datasource and models below,
// and ensure the DATABASE_URL environment variable is set correctly.

// datasource db {
//   provider = "postgresql" // Or "mysql", "sqlite", "sqlserver", "mongodb"
//   url      = env("DATABASE_URL")
// }

// generator client {
//   provider = "prisma-client-js"
//   // The following binaryTargets are for broader compatibility,
//   // especially in environments where libssl versions might vary.
//   // "native" should be prioritized.
//   // binaryTargets = ["native", "debian-openssl-1.1.x", "debian-openssl-3.0.x"]
// }

// model User {
//   id        Int      @id @default(autoincrement())
//   email     String   @unique
//   password  String
//   firstName String
//   lastName  String
//   phone     String? // Optional phone number
//   address   String[] // Array of strings for address lines
// }
